import AppLayout from "@/components/AppLayout";
import { Activity, BarChart3, BellRing, BrainCircuit, Download, Gauge, LockKeyhole, TrendingUp, Users } from "lucide-react";
import { toast } from "sonner";

const domains = [{ label: "Politics", real: 28, fake: 12, color: "bg-accent" }, { label: "Sports", real: 24, fake: 8, color: "bg-warning" }];
const languages = [{ label: "English", value: 54 }, { label: "Marathi", value: 31 }, { label: "Hindi / Mixed", value: 15 }];

export default function AdminPanel() {
  const exportReport = () => { const csv = "domain,real,fake\nPolitics,28,12\nSports,24,8"; const url = URL.createObjectURL(new Blob([csv], { type: "text/csv" })); const link = document.createElement("a"); link.href = url; link.download = "fake-news-analytics.csv"; link.click(); URL.revokeObjectURL(url); toast.success("Analytics CSV downloaded"); };
  return <AppLayout><div className="p-6 lg:p-8 max-w-[1300px] space-y-6"><div className="flex flex-col md:flex-row md:items-center justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.18em] text-accent font-semibold">Administration</p><h1 className="text-3xl font-display font-bold text-foreground mt-2">Fake News Detection admin</h1><p className="text-muted-foreground mt-1">Monitor models, trends, languages, and verification quality.</p></div><div className="flex gap-2"><button onClick={exportReport} className="inline-flex items-center gap-2 rounded-lg border border-input bg-card px-3 py-2 text-sm"><Download className="w-4 h-4" />Export CSV</button><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-lg bg-primary text-primary-foreground px-3 py-2 text-sm"><Download className="w-4 h-4" />Print / PDF</button></div></div><div className="grid grid-cols-1 md:grid-cols-4 gap-4">{[{ label: "Total scans", value: "72", icon: BarChart3 }, { label: "Likely fake", value: "20", icon: BellRing }, { label: "Average confidence", value: "85%", icon: Gauge }, { label: "Active users", value: "18", icon: Users }].map((item) => <div className="glass-card rounded-xl p-5" key={item.label}><item.icon className="w-5 h-5 text-accent" /><p className="text-sm text-muted-foreground mt-4">{item.label}</p><p className="text-2xl font-display font-bold mt-1">{item.value}</p></div>)}</div><div className="grid grid-cols-1 lg:grid-cols-2 gap-6"><div className="glass-card rounded-xl p-6"><h2 className="font-display font-semibold">Fake vs. real by domain</h2><p className="text-sm text-muted-foreground mt-1">Current month detection analytics</p><div className="space-y-5 mt-6">{domains.map((domain) => <div key={domain.label}><div className="flex justify-between text-sm mb-2"><span>{domain.label}</span><span>{domain.real + domain.fake} scans</span></div><div className="flex h-3 rounded-full overflow-hidden bg-muted"><div className="bg-success" style={{ width: `${(domain.real / (domain.real + domain.fake)) * 100}%` }} /><div className="bg-destructive" style={{ width: `${(domain.fake / (domain.real + domain.fake)) * 100}%` }} /></div><div className="flex gap-4 text-xs text-muted-foreground mt-2"><span>Real {domain.real}</span><span>Fake {domain.fake}</span></div></div>)}</div></div><div className="glass-card rounded-xl p-6"><h2 className="font-display font-semibold">Language distribution</h2><p className="text-sm text-muted-foreground mt-1">OCR detections by language</p><div className="space-y-5 mt-6">{languages.map((language) => <div key={language.label}><div className="flex justify-between text-sm mb-2"><span>{language.label}</span><span>{language.value}%</span></div><div className="h-3 rounded-full bg-muted"><div className="h-full rounded-full bg-accent" style={{ width: `${language.value}%` }} /></div></div>)}</div></div></div><div className="rounded-xl bg-primary p-6 text-primary-foreground"><div className="flex items-start gap-3"><TrendingUp className="w-5 h-5 text-warning mt-1" /><div><h2 className="font-display font-semibold">Trending fake-news alerts</h2><p className="text-sm text-primary-foreground/70 mt-2">Maharashtra watchlist topics with the most flags this week.</p><div className="flex flex-wrap gap-2 mt-4"><span className="rounded-full bg-primary-foreground/10 px-3 py-1 text-xs">Mumbai civic election claims</span><span className="rounded-full bg-primary-foreground/10 px-3 py-1 text-xs">IPL transfer rumours</span><span className="rounded-full bg-primary-foreground/10 px-3 py-1 text-xs">State policy screenshots</span></div></div></div></div><div className="glass-card rounded-xl p-6"><div className="flex items-center gap-3"><BrainCircuit className="w-5 h-5 text-accent" /><h2 className="font-display font-semibold">Model configuration</h2></div><div className="grid md:grid-cols-2 gap-4 mt-5"><label className="text-sm font-medium">Active model<select className="mt-2 w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm"><option>AsalKhabar Hybrid v1</option><option>OCR Linguistic v1</option><option>Regional Context v1</option></select></label><label className="text-sm font-medium">UI language<select className="mt-2 w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm"><option>English</option><option>Marathi</option><option>Hindi</option></select></label></div><div className="flex items-center gap-3 mt-6 p-4 rounded-lg bg-muted/50 text-sm text-muted-foreground"><LockKeyhole className="w-4 h-4 text-accent" />Settings are stored locally in this frontend-only build. <Activity className="w-4 h-4 text-success ml-auto" />Online</div></div></div></AppLayout>;
}
